#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	ll T;
    cin >> T;
    for (ll t=0; t<T; t++) {
        ll m, x;
        cin >> m >> x;
        ll arr[m][2];
        for (ll i=0; i<m; i++) {
            cin >> arr[i][0] >> arr[i][1];
        }

        ll arr[m][m*x];
        for (int i=0; i<m; i++) {
            for (int j=0; j<m*x; j++) {
                
            }
        }
    }
}
