#include <cstdio>

using namespace std;

int main()
{
    int n;
    scanf("%d", &n);
    int arr[n];
    int ans[n];
    int max=1;
    for (int i=0; i<n; i++)
    {
        ans[]
        scanf("%d", &arr[i]);
        if (i==0)
            ans[0]=1;
        else
        {

        }
    }
}