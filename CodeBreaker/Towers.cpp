#include <cstdio>
#include <vector>
#include <queue>
#include <utility>
#define ll long long

using namespace std;

int main()
{
    int n;
    ll d;
    vector <ll> h(n);
    priority_queue <
    for (int i=0; i<n; i++)
    {
        scanf("%lld", &h[i]);
    }
}