#include <cstdio>
#include <algorithm>
#include <vector>
#include <memory>

using namespace std;

int main()
{
    /*int n;
    scanf("%d", &n);
    vector <int> x(n);
    for (int i=0; i<n; i++)
        scanf("%d", &x[i]);
    sort(x.begin(), x.end());
    make_unique(x);
    for (int i=0; i<x.size(); i++)
        printf("%d ", x[i]);*/

    printf("%d", (3-0)>>1&1);
}